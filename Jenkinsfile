pipeline {
    agents any

    stages {
        stage('Sample Stage') {
            steps {
                sh /cat/os-release
                sh whoami
                sh date
            }
        }
    }
}